public String formatName(String firstName, String lastName) {
    //code here
    if (firstName != null && lastName != null) {
        return lastName + ', ' + firstName;
    } else {
        return ((firstName != null) && firstName != '') ? firstName : lastName;
    }
}