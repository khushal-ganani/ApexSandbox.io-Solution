public Boolean isPrime(Integer num) {
    //code here
    Boolean isPrime = true;
    for (Integer i = 2; i<num; i++) {
        if (Math.mod(num, i) == 0) {
            isPrime = false;
            break;
        }
    }
    return isPrime;
}