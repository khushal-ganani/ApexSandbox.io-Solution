public Integer fibonacci(Integer n) {
    //code here
    if (n>=1 && (n=1 || n=2)) {
        return 1
    } else if (n>=1 && n>2) {
        Integer fibonacciNumber = fibonacci(n-1) + fibonacci(n-2);
        return fibonacciNumber;
    }
}